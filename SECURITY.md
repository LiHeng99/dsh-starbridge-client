# 安全策略

这个插件会接触凭据：网关访问密钥、平台账号令牌、OIDC 刷新令牌。所以安全问题的报告方式
和"它到底怎么保管这些东西"都写在这里。

## 报告漏洞

**请不要开公开 issue。** 用仓库的 **Security → Report a vulnerability** 走私密报告
（GitHub Private Vulnerability Reporting）。如果你拿不到那个入口，就开一个只说
"我有一个安全问题要私下报告"的 issue，不要写细节，维护者会接上你。

请附上：受影响的版本、复现步骤、影响面（能读到什么 / 能改到什么），以及你已经验证过的部分。
**不要在报告里贴真实凭据**，用占位值。

## 这个插件怎么保管凭据

- **访问密钥**只写入 DSH 凭据库（`ctx.credentials`，引用名 `STARBRIDGE_GATEWAY_API_KEY`），
  不写任何配置文件。
- **平台令牌与刷新令牌**以 AES-256-GCM 加密落盘于 `$DSH_HOME`
  （`starbridge-gateway-session.enc`），密钥是同目录下 `0600` 的 `.key` 文件。
- **浏览器不持有 access token**，也不直接访问网关：所有网络请求、认证、重试与流式解析
  都在 host 面完成，页面只调用本插件自己的 `/starbridge/api/*`。
- **设置页的密钥与密码输入是只写的**：host 从不回传，页面无从回显。
- **日志统一走脱敏 sink**：`Bearer`、JWT、`*_token`、`api_key` 形状的文本一律替换。
- 网关返回与用户输入都按不可信文本渲染，Markdown 渲染器**不解释原始 HTML**。

## 它明确不做的事

- 不向任何第三方上报数据；反馈只写本地会话事件，并（可关地）转发给你自己配置的网关。
- 只读写自己那几份文件（`$DSH_HOME/starbridge-gateway.json` 与加密会话保险库），
  不扫描、不打包、不上传你的其他地方的文件。
- 只把用户通过工具或面板发起的内容发给网关；没有后台同步、没有遥测。
- `package.json` 里**没有任何安装期脚本**（没有 `postinstall` / `prepare`）：
  装它只下载 tarball，不会在你的机器上执行代码。

## 部署方需要注意

- 用 **https**；`http` 只在本地网关时被接受。
- 平台账号登录失败时后端只回"账号或密码不正确"，不区分用户是否存在（避免账号枚举）——
  如果你自己实现网关侧，请保持这个行为。
- **被插件市场收录不等于经过安全审查。** 收录只是形式校验，不构成对本插件或任何插件的
  安全审核、推荐或背书。
